{

    "buildCommand": "yarn build",
    "framework": "nextjs",
    "installCommand": "yarn install",
    "functions": {
      "src/pages/api/**/*.ts": {
        "memory": 1536,
        "maxDuration": 10000,
        "runtime": "nodejs18.x"
      }
    }
}
